SELECT 
    TRANS.TICKET_NO, 
    TRANS.DATE_OUT, 
    PRODUCT.PROD_NAME, 
    TRADER.TRADER_NAME, 
    TRANS.NET_WT
FROM TRANS
JOIN PRODUCT 
    ON TRANS.PROD_CODE = PRODUCT.PROD_CODE
JOIN TRADER
    ON TRANS.TRADER_CODE = TRADER.TRADER_CODE;