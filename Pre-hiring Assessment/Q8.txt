SELECT 
    TRANS.DATE_OUT, 
    TRANS.TRADER_CODE, 
    TRADER.TRADER_NAME, 
    TRANS.PROD_CODE, 
    SUM(TRANS.NET_WT)
FROM TRANS
JOIN TRADER
    ON TRANS.TRADER.CODE = TRADER.TRADER_CODE
GROUP BY DATE_OUT AND TRADER_CODE AND PROD_CODE;